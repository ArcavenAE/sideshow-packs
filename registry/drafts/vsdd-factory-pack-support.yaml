# Pack-support register — vsdd-factory (DRAFT, unvalidated)
# Schema v0.2.0 fields demonstrated; see docs/pack-support-candidates.md.
# Moves to registry/ proper when first validated bracket lands.

kind: pack-support
schema_version: 0.2.0
status: draft-unvalidated
pack: vsdd-factory

upstream:
  repo: https://github.com/drbothen/vsdd-factory
  default_branch: develop        # gitflow — HEAD is NOT release content
  release_notes: https://github.com/drbothen/vsdd-factory/releases
  content_root: plugins/vsdd-factory   # Claude plugin dir = the pack surface

version_scheme: semver-releases
release_line: prerelease         # active line is v1.0.0-rc.N; stable line
                                 # dormant at v0.79.4 (2026-04-24)

# Targeting (analysis 2026-07-12): v1.0.0-rc.22. Plugin content went
# through heavy rework rc.18 -> rc.20 (176 plugin files changed) and
# has been FROZEN rc.20 -> rc.22 (0 plugin files across 54 commits —
# all churn is factory/Rust side). rc.22 = newest tag carrying the
# stable plugin content. Prototype-bundle validation brief:
# aae-orc docs/briefs/vsdd-factory-prototype-bundle-brief.md (skippy).
target: v1.0.0-rc.22

acquisition:
  method: direct-tree            # no upstream installer for content;
                                 # capture plugins/vsdd-factory at tag.
                                 # NOTE: releases carry NO assets (tag +
                                 # notes only) — acquisition is git tree
                                 # at tag, never release-asset download.

redistribution: public-ok        # MIT per .claude-plugin/plugin.json
                                 # @ rc.22 (confirmed 2026-07-12)

activation:
  default_scope: per-repo
  per_repo_required: true
  rationale: >-
    Ratified 2026-07-12: because of how vsdd-factory works, it must be
    enabled/disabled per repo only, managed by sideshow. Content MAY
    install to user directories, but is never active by default at
    user scope. Enforcement is sideshow-side (Binding abstraction,
    aae-orc-f13j); this register is the declaration both sides read.

pipeline_assumptions:
  - id: plugin-dir-exists
    desc: plugins/vsdd-factory exists at the captured tag and is the complete content surface
  - id: plugin-manifest-shape
    desc: Claude plugin manifest parses; binding names/prefixes enumerable for per-repo enable/disable
  - id: no-build-required
    desc: content capture requires no cargo build (content is not compiled output)

validated: []                    # nothing validated yet

wrinkles:
  - id: release-lines-diverged
    introduced_in: 0.79.4
    resolved_in: null
    severity: version-selection
    summary: >-
      Stable line dormant since v0.79.4 (2026-04-24); development ships
      as v1.0.0-rc.N prerelease GitHub Releases (rc.22 @ 2026-07-03).
      "Package vsdd-factory X" is ambiguous without naming the line.
    adaptation: release_line field declares which line brackets apply to.
    references:
      - gh release list -R drbothen/vsdd-factory (2026-07-12 observation)

  - id: gitflow-develop-head
    introduced_in: null
    resolved_in: null
    severity: acquisition-caution
    summary: >-
      Default branch develop is ahead of any release; capturing HEAD
      instead of a tag packages unreleased content.
    adaptation: acquisition always by explicit tag, never branch HEAD.
    references:
      - repo defaultBranchRef=develop (2026-07-12 observation)

  - id: plugin-carries-executables
    introduced_in: null
    resolved_in: null
    severity: content-class
    summary: >-
      The plugin ships executable content — bin/, tools/, hooks/ with
      hooks-registry.toml + resolvers-registry.toml, hook-plugins/ —
      not just markdown. Hooks active at user scope would fire in every
      repo; this is the mechanical reason behind the per-repo
      activation mandate. Executable content may also carry
      signing/verification implications a markdown-only pack doesn't.
    adaptation: >-
      activation.per_repo_required=true; prototype-bundle validation
      must enumerate hooks/binaries and confirm per-repo enable/disable
      actually scopes them.
    references:
      - plugins/vsdd-factory tree @ v1.0.0-rc.22 (2026-07-12 observation)

  - id: plugin-version-vs-content-drift
    introduced_in: null
    resolved_in: null
    severity: provenance-caution
    summary: >-
      Plugin content frozen rc.20 -> rc.22 while the repo tags advanced;
      .claude-plugin/plugin.json version reads 1.0.0-rc.22 at the rc.22
      tag. Bundle version should follow the CAPTURED TAG, with content
      sha in the manifest, so identical-content bundles at different
      tags remain distinguishable and honest.
    adaptation: file-manifest.csv + tag recorded in install.meta per pipeline standard.
    references:
      - compare v1.0.0-rc.20...v1.0.0-rc.22 plugin_files=0 (2026-07-12)

llm_runbook: registry/pack-support-revalidation-runbook.md
